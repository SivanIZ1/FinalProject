[pytest]
# Register custom markers to avoid PytestUnknownMarkWarning
markers =
    sanity: core critical path automation tests
    smoke: high-level smoke tests for rapid build verification
    regression: full suite verification coverage tests
    error_handling: negative validation and boundary input tests
    homepage: layout and core landing view validation tests

# Default execution rules
addopts = --strict-markers -v